meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{ServerUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "emailOrUsername": "test",
    "password": "1234"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
